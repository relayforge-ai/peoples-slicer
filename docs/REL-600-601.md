# REL-600 / REL-601 — universal slice-and-send

| Ticket | Focus |
|--------|--------|
| **REL-600** | `refit`, profile harvester, plate policy goals, 3×/wk harvest timer |
| **REL-601** | Land slice into MIT `forge` CLI: `forge slice` / `forge slice-send` / `forge harvest` |

## Safety

- Live send still requires `--bed-confirmed` (guardian fail-closed).
- Plate cycler gcode is **not** invented — drop verified C1M `plate_change_gcode` when Ryan has it.
- No LAN secrets in this package; AppImage paths via env (`BAMBU_STUDIO_BIN`, `ORCA_ROOT`, `BAMBU_PROFILES`).

## Tests

```bash
pytest tests/test_slice_rel600_601.py tests/test_cli.py -q
```
